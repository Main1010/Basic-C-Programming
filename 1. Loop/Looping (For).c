
#include <stdio.h>
int main()
{
    int i; // Declaration.
    for(i = 1; i <= 10; i++) // (Initialize ; Condition ; Increment/Decrement):- Statement.
    {
        printf("%d\n",i);
    }
    return 0;
}

// i is accountant it will always build blocks to put input.
// In [for loop] it will always run unless the condition not false.
